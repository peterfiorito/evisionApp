# eVision balance API webApp

Build in angular.js
To correctly display it run a local server with node.
npm install http-server -g and run http-server . -o in app directory.

Of course, it also needs the provided api server.

